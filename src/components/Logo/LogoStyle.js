import styled from "styled-components";

export const LogoStyle = styled.text`
    font-size: 2rem;
    font-weight: 800;
    color: white;

    strong{
        color: var(--blue);
        font-size: 4rem;
    }

    line-height: 1.4rem;

    margin-right: 15px;
`